{
  "name": "functions",
  "description": "Cloud Functions for NEP membership id generation",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint --ext .ts src || true"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.6",
    "eslint": "^8.0.0",
    "@types/node": "^18.0.0"
  }
}
